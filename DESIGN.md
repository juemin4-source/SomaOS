# Design: SomaOS — 以能力为第一公民的工作运行时

Generated by /office-hours on 2026-07-24
Branch: main
Repo: AI-Claude-Foundry
Status: APPROVED
Mode: Builder
Supersedes: liuyiyu-main-design-20260722-171525.md (Agent 平行会议系统)

## Problem Statement

现有 LLM 编码 Agent CLI（Claude Code、Goose、OpenCode 等）以模型为中心、会话为连续性载体、编码仓库为主要世界。能力寄生在模型上下文中，完成判断依赖模型自我评估，权限停留在工具级别，状态随会话消失。

SomaOS 的目标是反转这个模型：**以工作世界为中心**——能力独立于任何模型存在，状态与证据有独立实体，Combo 在运行时显式生长，完成由证据裁决。

这不只是"另一个 Agent CLI"。它是一套允许 AI 获得不同身体、组织不同工作并对现实结果负责的通用工作操作系统。

0.1 的切入产品是 **Soma Case**——一个以"工作案件"而非聊天会话为中心的本地代码问题调查与修复系统。

## What Makes This Cool

六层差异化：

1. **能力（Softill）是独立资产**——不属于任何模型。Claude、GPT、固定程序、Combo 都可以调用。它有明确的输入、输出、副作用、权限、感觉、证据和失败语义。

2. **工作结构在运行时生长**——Combo 不是静态流水线，而是每次任务中真正长出来的执行形态。同一 Skill 在不同上下文中形成不同的能力链。

3. **状态不属于聊天历史**——现实事实、用户决定、证据、作品、权限——每种状态显式分类，有来源、作用域和有效期。

4. **权限针对能力—对象—目的**——不只是"能不能调 Bash"。是谁、为了哪个目标、通过哪项能力、对哪个对象、执行什么副作用、凭什么授权。

5. **完成由证据裁决**——任何对"完成"的主张必须绑定具体证据链：对应 Commit、复现结果、修复验证、回归通过、真实环境确认。

6. **世界不限于代码**——同一套 Skill → Combo → Capability → Organ → Evidence 可以用于代码调查、Blender 建模、ComfyUI 工作流、Figma 设计、数据库变更、生产验证。

## Constraints

- 0.1 只做一个 Skill：**investigate and repair**（调查与修复）
- 只支持本地 Git 代码项目
- 核心语言：**Rust**；Rig 作为首个可替换 Provider 适配层
- 桌面端：Tauri + React + TypeScript（0.2 以后）
- 0.1 形态：CLI，Core 在进程内运行（不要求 Daemon）
- 不做：通用桌面操作、浏览器 Agent、Blender/ComfyUI、多 Agent 公司、Skill 商店、IDE 插件、云同步
- 核心 crate 边界：`soma-cli` → `soma-core` → `soma-model`/`soma-capability`/`soma-store`

## Premises

1. **Case 模型是 0.1 正确的产品楔子。** 能力、权限、状态、行动和证据需要共同的归属实体；Session 语义不够强。
2. **Rust 核心的成本可接受。** 即使第一条模型交互比 TypeScript 路线晚几周，但 Capability、Action、Evidence 等系统事实需要强类型、可序列化、原子持久化的运行时纪律。
3. **Rig 只做 Provider 适配层。** 不拥有 Tool Execution、Context Policy、Permission、Session Truth、Evidence 和 Completion。
4. **ActionRequest 是正式实体。** 模型不能直接执行工具——每次真实行动产生 ActionRequest → PolicyCheck → Permission → Execution → Observation → Evidence 的事件链。
5. **代码修改后相关证据自动失效。** 必须重新验证才能进入 RESOLVED。
6. **多模型路由是 SomaOS 的责任。** Rig 不决定用哪个模型。
7. **组织多样性不要求核心用 TypeScript。** Python/Node 是边缘能力（Sidecar Organ），不影响核心选型。

## Approaches Considered

### Approach A: Case-first Soma Core（推荐）

Rust 核心 + Rig Adapter + 自写 Turn Engine。以 Case 为第一产品实体，`soma investigate` 为 0.1 Skill。通过 M0–M5 内部里程碑分段交付。

- 努力程度: M–XL（取决于分段边界）
- 风险: Med（Turn Engine 和 Provider 差异需要扎实工程）
- 优点: 产品差异第一天确立；架构干净无迁移债务；Case 模型不可逆建立工作连续性
- 缺点: 第一条可展示模型交互比 TypeScript 慢；Provider 差异的坑要亲自踩

### Approach B: 通用 Skill runner

先做 `soma run <skill>` 框架，Case 模型放到 0.2。

- 努力程度: M
- 风险: Low–Med
- 优点: 0.1 就验证多 Skill 可插拔；更早给出可运行产物
- 缺点: 产品差异延后；容易滑回 Session 模型；0.2 补 Case 更难

### Approach C: 双内核渐进路线

TypeScript 用 pi-ai 快速验证 Case 模型，Rust 核心并行写。

- 努力程度: XL（双倍维护）
- 风险: High（注意力分散；TypeScript 原型可能成为生产代码）
- 优点: 快速验证产品假设；不阻塞在 Rust 基建
- 缺点: 两套事实源、两套协议、长期迁移惯性

## Recommended Approach

**Approach A: Case-first Soma Core**，以 M0–M5 内部里程碑分段落地。

理由：Case 是权限、状态、行动、证据和恢复的共同归属实体，不能作为 0.2 的附加功能补入。Approach B 在验证一个完整 Skill 之前过早抽象 Skill runner，并会默认建立 Session-first 架构。Approach C 双内核制造两套事实源和迁移惯性，不符合当前注意力预算。

## M0–M5 Internal Milestones

### M0: Turn Engine + 单模型 Tool Call 闭环（含最小可见原型）

M0 的验收标准就是最早的可见产品交互，而非基础设施里程碑。

**最小可见原型（M0 验收标准）：**

1. CLI 接受 `soma investigate` + 问题文本（`soma` 二进制可运行）
2. 创建 Case（持久化到 JSON 文件，带真实 case_id）
3. Turn Engine 发起一次模型请求
4. 模型返回 Tool Call，系统暂停并打印 ActionRequest（含 Action ID、Capability、参数）
5. 用户输入 Observation 后恢复
6. 模型输出最终回答
7. 进程可终止并重新运行

**M0 完整交付物：**
- SomaTurnEngine 核心：receive(ContextView) → request_model() → emit(EventEnvelope) → suspend_on(ActionRequest) → receive(Observation) → continue_turn() → finish(ClaimProposed)
- Rig Adapter 对接 Claude Provider
- 统一 EventEnvelope 协议（含完整 Action 生命周期事件）
- CLI 交互模型：模型流式输出实时打印 → ActionRequest 时暂停 → 打印 Action ID + "等待授权" → 用户输入 Observation → 恢复 Turn
- 所有事件包含真实 case_id

### M1: Basic Case + 文件/Shell Organ

- Case 实体：ID、目标、状态、假设、权限、证据
- SQLite 持久化（或事件日志文件）
- File Organ：read、search
- Process Organ：run（白名单命令）
- Git Organ：status、diff、log
- **验证标准：** 可以 `soma investigate "问题描述"` 创建 Case，Agent 能读取文件、搜索代码、运行 Git 命令

### M2: Permission + Action Trace

- ActionRequest → PermissionCheck → PermissionResolved 链路
- 写入前授权（file.patch 必须暂停等待 Owner）
- Shell 风险分类（安全/警告/拒绝）
- Action Trace：每次 Action 的完整事件记录
- 写入范围限制（不允许越出项目仓库）
- **验证标准：** Agent 写入文件前必须等待用户授权；越范围写入被阻断；Trace 可回放

### M3: Evidence + 验证失效

- Evidence 分类：

  | 类型 | 语义 | 新鲜度敏感 | 样例 |
  |------|------|-----------|------|
  | OBSERVATION | 模型观察到的原始事实（日志、代码内容、运行时行为） | 否——历史记录永久有效 | "请求返回了 404" |
  | DIAGNOSIS | 模型对根因的判断 | 否——历史诊断记录永久有效 | "路径前缀不匹配" |
  | CHANGE | 代码修改记录 | 是——当代码变化后失效 | "已将路径从 /api/profile 改为 /api/v1/profile" |
  | VERIFICATION | 验证执行的结果 | 是——依赖当前代码状态 | "修复后测试通过" |
  | EXTERNAL_CONFIRMATION | 外部确认（Owner 验收、第三方报告） | 否——永久有效（除非明确作废） | "Owner 确认修复有效" |

- 每条 Evidence 包含：evidence_id, case_id, evidence_type, subject, provenance, producer_action_id, workspace_snapshot (commit_hash + file_paths + content_hashes), freshness_policy (STALE_ON_CODE_CHANGE / PERSISTENT), supports_claim
- 验证失效机制：
  - CHANGE 和 VERIFICATION 类型 Evidence 使用 freshness_policy = STALE_ON_CODE_CHANGE
  - 代码修改时，对比 workspace_snapshot 中的 content_hashes；发生变化时，仅 STALE_ON_CODE_CHANGE 类型的 Evidence 被标记 STALE
  - OBSERVATION、DIAGNOSIS、EXTERNAL_CONFIRMATION 始终 PERSISTENT，不会被自动 stale。根因说明可以引用修复前的历史证据。
- **"所有 ResultClaim 只能引用新鲜 Evidence" 修正为：对于 CHANGE 和 VERIFICATION 类型的 Evidence，必须新鲜；OBSERVATION 和 DIAGNOSIS 类型的 Evidence 不受此限制。**
- 裁决状态：RESOLVED / BLOCKED / UNVERIFIABLE / NEEDS_DECISION
- **验证标准：** 修改代码后原有修复证据失效；需重新验证才能 RESOLVED

### M4: Recovery + 完整性验收

- 进程中断恢复：重启动后 `soma resume SOMA-0001` 恢复 Case
- 已产生副作用的动作不得无意识重复——action 写入 store 前标记 "execution_started"（含 idempotency_key），执行完成后标记 "committed"。crash 在 execution_started 和 committed 之间的 action 在恢复时标记为 UNCERTAIN，通过 effect_readback（回读文件是否已修改、命令是否已生效）判断是否需要重试。
  - 目标：可判定、可回读、可恢复的副作用执行。不追求通用 exactly-once。
- 换模型继续同一 Case（`soma resume SOMA-0001 --model gpt-5`）
- 换模型继续同一 Case（`soma resume SOMA-0001 --model gpt-5`）
- 完整用户路径跑通：创建 → 调查 → 修复 → 验证 → RESOLVED
- **验证标准：** 成功标准 1~6 全部通过（中断恢复、换模型继续、重复副作用防护、完整用户路径）

### M5: Fixture QA + 证据绑定

- 在真实 Fixture Repo 植入三类问题（逻辑 Bug、配置 Bug、运行时复现 Bug）
- 每类问题至少 2 例
- 完整 E2E 测试 5 次重复运行
- 保存 Transcript、Action、Cost、Turns、Evidence
- 统计每类问题成功率
- **验证标准：** 三类问题均达到 >=60% 修复成功率；5 次 E2E 完整运行；每类问题至少 2 例；Transcript/Action/Cost/Turns/Evidence 全部保存

## Core Concepts

### Case 状态机（拆分为三个维度）

三个正交维度，不再混合不同语义：

**lifecycle**: `ACTIVE | SUSPENDED | CLOSED`

**phase**（当前工作阶段）: `INVESTIGATING | REPAIRING | VERIFYING`

**resolution**（最终裁决）: `NONE | RESOLVED | BLOCKED | UNVERIFIABLE | NEEDS_DECISION`

有效组合示例：
- `ACTIVE / INVESTIGATING / NONE` — 正常调查中
- `ACTIVE / REPAIRING / NONE` — 修复中
- `SUSPENDED / INVESTIGATING / NEEDS_DECISION` — 等待 Owner 决策挂起
- `CLOSED / INVESTIGATING / RESOLVED` — 已关闭的已解决 Case
- `CLOSED / INVESTIGATING / BLOCKED` — 不可恢复的阻塞

**Hypothesis** 是独立实体，不是 Case 状态：

```
Hypothesis {
    h_id,
    statement,
    supporting_evidence: Vec<EvidenceId>,
    falsification_method,
    status: ACTIVE | FALSIFIED | CONFIRMED,
}
```

一个 Case 可以有多个 Hypothesis 同时存在。FALSIFIED 的 Hypothesis 保留为历史记录，不删除。

### ContextView

Turn Engine 发送给模型的当前上下文物件。包含：
- **System prompt**: 当前 Skill、Case 目标、权限范围
- **Message history**: 当前 Turn 内的模型和用户消息（不含过期证据）
- **Fact block**: 当前活跃证据摘要（只含结论、来源 ID、新鲜性标记）
- **Action record**: 本轮已执行的 Action 及其 Observation

ContextView 每次 Turn 从 Soma Store 重建，不依赖 Provider Session 的隐含历史。

构成：
- **当前 Case 目标**（来自 Case.lifecycle + Case.phase）
- **有效权限**（当前 ACTIVE 且未过期的 Permission）
- **活跃假设**（Hypothesis status=ACTIVE）
- **当前事实**（OBSERVATION + DIAGNOSIS 类型的 Evidence）
- **待处理 Action**（已 ActionExecutionStarted 但未 Committed 的 Action）
- **有界的近期消息**（最近 N 轮模型/用户交互，由 Context Policy 限定）

Provider-native context 可作为临时缓存，不作为事实源。

### Completion State

Turn Engine 完成一轮推理后的决策状态。包含：
- **result_claim**: 模型主张的结论
- **fresh_evidence_ids**: 结果依赖的新鲜证据
- **unverified_claims**: 模型声称但无新鲜证据支持的部分
- **action_summary**: 本轮执行的所有 Action 及其效果
- **state_transition**: 建议的 Case 状态转移（由 Soma Control Plane 裁决，模型建议不等于最终状态）

### Contract（Capability Contract）

Capability 向 SomaOS 注册时的正式声明。包含：

- **capability_id**: 唯一名称（如 `file.read`）
- **input_schema**: 输入参数的 JSON Schema
- **output_schema**: 输出结果的 JSON Schema
- **permission_policy**: 执行所需的权限声明（不再是 READ/WRITE/DANGEROUS 三级）
- **effect_class**: 副作用分类（READ_ONLY / MUTATE_LOCAL / MUTATE_EXTERNAL / DESTRUCTIVE）
- **scope_resolver**: 运行时解析真实作用范围的函数（参数 Case root + Action params → 绝对路径集合）
- **reversibility**: 是否可撤销（REVERSIBLE / IRREVERSIBLE / CONDITIONAL）
- **idempotency**: 幂等性声明（IDEMPOTENT / NOT_IDEMPOTENT / IDEMPOTENT_WITH_KEY）
- **timeout_policy**: 默认超时与最大超时
- **cancellation_support**: 是否支持优雅取消
- **evidence_schema**: 执行后生成的 Evidence 结构声明
- **side_effects**: 已知副作用的枚举

### Organ 与 Capability 的关系

```
Capability（能力契约，对外可见）
    └─ Organ（底层实现，SomaOS 管理）
          ├─ File Organ: 文件读写搜索
          ├─ Process Organ: Shell 执行
          └─ Git Organ: 版本控制操作
```

Capability 定义"能做什么"（声明层面）。Organ 执行"怎么做"（实现层面）。
多个 Capability 可以共享同一个 Organ（如 `file.read` 和 `file.search` 都使用 File Organ）。

在 0.1 中，"Investigate and Repair" 是一个 **Skill**（方法），它编排一组 **Capability**（能力），每个 Capability 由 **Organ** 执行。

## Core Architecture

```
soma-cli
   │
   ▼
soma-core
├─ Case
├─ Turn Engine
├─ Event Protocol
├─ Context View
├─ Completion State
└─ Recovery
   │
   ├──────────────┬────────────────┐
   ▼              ▼                ▼
soma-model     soma-capability   soma-store
├─ ModelTrait   ├─ Contract       ├─ SQLite
└─ Rig Adapter  └─ Executor       └─ Event Log
                      │
             ┌────────┼────────┐
             ▼        ▼        ▼
           Files    Process    Git
```

**边界规则：**
- Core 定义 Port：`ModelProvider`、`CapabilityRuntime`、`CaseStore` 由 `soma-core` 定义 trait。具体实现（Rig Adapter / SQLite Store / Native Organs）是 Adapter。Core 不直接依赖任何 Adapter crate。
- Rig 类型只能存在于 `soma-model-rig` 边界 crate，绝不泄漏到 core。
- Tool Execution 由 `soma-capability`（SomaOS 控制面）全权管理。模型返回的 ToolCall 必须由 Core 转换成 `ActionRequest` 后才进入验证 → 权限 → 持久化 → 执行链。模型 ToolCall 不是正式 Action。
- `soma-store` 提供 Case、Action、Evidence、Decision 的持久化。先以 SQLite 或事件日志实现。

## Event Protocol

所有事件使用统一信封，支持 Trace、回放、恢复和迁移。

```
EventEnvelope {
    event_id: UUID,
    case_id: String,
    sequence: u64,             // case-local monotonic counter

    envelope_version: u16,     // 信封结构版本（极少变化）
    event_type: String,        // "action.requested", "evidence.recorded" ...
    event_version: u16,        // 该事件类型的 payload 版本

    timestamp: DateTime,
    actor: "model" | "soma" | "owner" | "system",
    causation_id: Option<UUID>, // 触发此事件的事件 ID
    correlation_id: UUID,       // 同一逻辑链共享

    payload: serde_json::Value, // 原始 payload，解码时按 (event_type, event_version) 选择 Upcaster
}
```

事件存储：forward-only append，历史记录永不原地修改。
解码路径：SQLite 原始事件 → Envelope Decode → (event_type, event_version) → EventCodec Upcast → Current DomainEvent。
Core 只处理当前领域类型，历史兼容逻辑封装在 EventCodec 中。
未知事件版本必须 Fail Closed（UNSUPPORTED_EVENT_VERSION）。
仅影响展示的可选字段可保持版本；影响状态/权限/副作用/验证/恢复的变化必须升 event_version。

EventPayload 枚举：

```
// == Turn Lifecycle ==
TurnStarted            { model, context_view_snapshot }
ModelTextDelta         { content }
ModelReasoningDelta    { content }
UsageUpdated           { input_tokens, output_tokens, cached_tokens, reasoning_tokens }
TurnSuspended          { reason: ActionPending | OwnerGate | Cancelled | Timeout }
TurnResumed            { context_delta }
TurnCompleted          { finish_reason: Stop | MaxTurns | Complete | Cancelled }
TurnFailed             { error_type, error_detail }

// == Action Lifecycle（完整链）==
ActionRequested        { capability_id, parameters, context_digest }
PolicyEvaluated        { decision: Allow | Deny | NeedsOwner, policy_used }
PermissionRequested    { permission_id, scope_resolved, owner_gate }
PermissionGranted      { permission_id, granted_by }
PermissionDenied       { permission_id, reason }
ActionExecutionStarted { }
ActionExecutionCommitted { result_hash }
ActionExecutionFailed  { error_stderr, exit_code }
ActionExecutionUncertain { reason }  // crash after started but before committed

// == Observation & Evidence ==
ObservationAccepted    { action_id, observation, evidence_ids }
EvidenceRecorded       { evidence_id, evidence_type, subject, producer_action_id }
EvidenceStaled         { evidence_id, staled_by_action_id }

// == Hypothesis ==
HypothesisProposed     { h_id, statement, supporting_evidence_ids }
HypothesisFalsified    { h_id, falsified_by_evidence_id }
HypothesisConfirmed    { h_id }

// == Claim & Adjudication ==
ClaimProposed          { claim_text, evidence_ids }    // 由模型提出
ClaimAdjudicated       { claim_id, status: Accepted | PartiallySupported | Rejected | Unverifiable, reasoning } // 由 Soma Control Plane 裁决

// == Case Lifecycle ==
CaseStateChanged       { lifecycle, phase, resolution }
```

## 并发模型

- Soma Core 运行在 Tokio Runtime 中
- Model Provider 以 async task 流式输出
- stdin 使用独立 blocking thread，通过 channel 发送 ClientCommand
- 所有 Case/Turn/Action 状态只由 Core Event Loop 修改
- Renderer 是 stdout 的唯一写入者
- TurnSuspended 是持久化领域状态，不以线程阻塞表达

0.1 的 error handling 保持简单：不自动重试，失败即上报。

| 层级 | 失败类型 | 0.1 行为 |
|------|---------|---------|
| Provider | RateLimit, Auth, Timeout, InvalidRequest | TurnFailed 事件 → Case 进入 BLOCKED，附带错误原文 |
| Turn Engine | 非法 Tool Call 参数、序列化错误 | TurnFailed → BLOCKED |
| Capability | Organ 执行失败（文件不存在、命令退出码非零） | Action 标记 FAILED，Observation 包含 stderr，继续 Turn |
| Permission | 越范围、未授权 | Action 标记 DENIED，继续 Turn |
| Evidence | 写入失败 | TurnFailed → BLOCKED |

不做：自动重试、自动降级、自动切换 Provider。这些在 0.2 以 Policy 形式加入。

## Undo / Rollback（0.1 策略）

Agent 每次文件修改 Action 自动保存：

- **touched_paths**: 本次修改涉及的所有文件路径
- **preimage**: 每个文件的修改前完整内容（文件不存在时标记）
- **postimage**: 每个文件的修改后完整内容
- **action_id**: 关联的 Action ID

用户可通过 `soma undo <action_id>` 恢复该 Action 触及的文件到 preimage。

安全规则：
- 若文件在 Action 后被用户或其他进程修改（content hash 与 postimage 不一致），拒绝自动撤销，进入冲突处理（由用户手动决定）。
- 不实现多步一键 undo、不实现 Shell 副作用的自动回退。

不采用全量 git diff 方案的原因：当前工作树可能已有用户独立修改；整体反向 diff 会连用户修改一起撤销。

## Test Strategy

### M0 测试分层（PR Gate）

1. **Turn Engine Unit Tests** — Mock ModelProvider，完整状态转移和不变量测试。作为 PR Gate。
2. **Core Integration Tests** — Scripted/Fake ModelProvider + In-memory Store，验证流式事件、暂停、Observation、恢复和完成顺序。作为 PR Gate。
3. **CLI Subprocess Smoke** — Spawn 真 CLI，piped stdin/stdout，使用 Fixture Provider，一条确定性 Golden Path。作为 PR Gate。
4. **Rig + Claude Real Smoke** — 验证真实 Provider 适配。M0 验收与 Adapter 变更时运行，不作为常规阻断 Gate。

### 后续里程碑（M1–M5）

- **Unit**: Turn Engine 状态机、Event 序列化、Permission 逻辑、Evidence 生命周期
- **Integration**: Capability + Organ 端到端（读写文件、运行命令、Git 操作）——真实文件系统
- **E2E / Fixture QA**: M5 标准——5 次独立运行，统计成功率

M0–M4 每个 milestone 完成时附带对应层级的测试覆盖率。

## Reviewer Concerns（独立审阅者遗留问题）

以下问题已在审阅中被识别，当前迭代未完全解决。作为已知缺口留档，供实现阶段参考：

1. **长期愿景与 0.1 范围的张力。** 文档标题描述"工作运行时"，但 0.1 交付的是一个 CLI 调查工具。六个差异层中部分在 0.1 不可见。可以接受——这是路线图问题，不是设计缺陷。

2. **Capability/Organ 抽象在 0.1 中过早。** 对于一个 Skill + 三个 Organ，这套抽象层可能过度。已知。保留理由：从第一天建立能力契约，避免后续重构。

3. **文件级 Evidence 失效可能导致 RESOLVED 死锁。** 调查和修复操作在同一文件上时，修复会 invalidate 调查证据。0.1 接受——用户可手动重新验证关键证据。

4. **At-most-once 语义与 M5 指标存在冲突。** 如果 crash 静默丢失 action，M5 的成功率衡量会失真。0.1 接受——crash 场景在 M5 统计中单独标记。

5. **缺少 Plugin / Skill 实体的架构位置。** "Skill"作为核心概念出现但不在 crate 划分中。实现阶段决定：Skill 是运行时组合策略，不是编译期实体。

6. **CLI 命令体系未定义。** 0.1 只有 `soma investigate` 和 `soma resume`。更多命令在 0.2 定义。

## Open Questions 先用哪个 Provider 验证？Claude（最熟悉）还是先跑通一个便宜的模型？
2. `soma-store` 用 SQLite（0.1 决策）。理由：ACID + 简化 crash recovery。事件日志作为可选格式放在 0.2 评估。
3. 0.1 的 Rust 项目结构：workspace 怎么切？`soma-core`、`soma-model`、`soma-capability`、`soma-store` 各放独立 crate？
4. 周道编译链与 Rust 类型的衔接——这个先放 0.2，还是 M5 之前做一个最小实验？
5. Rust CLI 的参数解析框架选哪个？clap 是共识，但要确认 windows 兼容性。
6. **Context Budget 策略（已决策）：** ContextView 从 Soma Store 重建。provider-native context 只做缓存。超出限制时优先截断近期消息窗口，仍超限则进入 NEEDS_DECISION。0.2 再设计摘要 checkpoint。
7. **写入范围边界：** 0.1 在 Case 创建时从 cwd 获取 repo root；所有文件操作约束在此路径下；symlink 指向 repo 外的路径不跟踪（视为外部引用，拒绝写入）。多 repo Case 暂不支持。

## Success Criteria（0.1 门禁）

**系统不变量（必须 100%）：**

1. 越权写入（超出 Case scope 或未授权）必须被阻断——自动测试验证。
2. 权限请求不得跳过——自动测试验证。
3. Evidence 失效不得漏掉既定测试场景——自动测试验证。
4. Crash 后不得静默重复已 committed 的副作用——crash harness 验证。
5. ClaimProposed 必须经 ClaimAdjudicated 后才能成为 Case 最终状态——自动测试验证。

**模型质量指标（统计基线，>=60%）：**

6. Fixture Repo 三类植入问题（逻辑 Bug、配置 Bug、运行时复现 Bug）的修复成功率 >= 60%。
7. 定性评估：用户一句自然语言能够创建 Case，模型能独立完成调查并产出可证伪假设。
8. 定性评估：最终结论引用具体 Evidence（证据 ID + 摘要），区分已证明、推断和未验证内容。

## Distribution Plan

- GitHub 发布源码 + 预构建二进制（Windows x64 优先）
- GitHub Actions CI：每次 PR 运行 unit + E2E fixture
- `cargo install soma-cli` 后续支持
- 不要求 Node/Python 运行时；Organ sidecar 按需安装

## Next Steps

### Implementation Tasks（from Eng Review）

| ID | Pri | Title | Source | Files | Verify |
|----|-----|-------|--------|-------|--------|
| T1 | P1 | Ports & Adapters 骨架：定义 ModelProvider/CapabilityRuntime/CaseStore trait | D2 | soma-core/src/port/ | 编译通过 |
| T2 | P1 | EventEnvelope 双版本化结构 + EventCodec 模块 | D4 | soma-core/src/event/ | Unit: roundtrip 序列化 |
| T3 | P1 | Async 架构：Tokio Runtime + blocking stdin thread + channel | D3 | soma-cli/src/main.rs, soma-core/src/engine/ | Integration: 流式暂停恢复 |
| T4 | P1 | 最小可观测性底座：tracing + 三分流 + 目录 + SQLite schema_version | D6 | soma-core/src/observability/ | 日志不泄漏 secret |
| T5 | P2 | CLI Subprocess Smoke Test（Fixture Provider） | D5 | soma-cli/tests/ | Github CI PR Gate |
| T6 | P2 | 敏感信息默认防护 + log audit | D6 | soma-core/src/observability/ | 自动测试 |
| T7 | P3 | soma doctor 接口原型 | D6 | soma-cli/src/commands/doctor.rs | 输出基本信息 |

### Milestone 顺序

1. **M0**: T1→T2→T3→T4→T5（Core 闭环 + 可观测性底座 + 自动化回归）
2. **M1**: Case 实体 + 文件/Git/Shell Organ
3. **M2**: Permission + Action Trace
4. **M3**: Evidence + 验证失效
5. **M4**: Recovery + 完整性验收
6. **M5**: Fixture QA + 证据绑定

<!-- "What I noticed about how you think" 已移除——不属于 canonical design。保留于 office-hours 会话记录。 -->

## Observability & Operations（M0 最小架构）

### 三种输出严格分离

| 类别 | 用途 | 存储 | 可回放 | 示例 |
|------|------|------|--------|------|
| Domain Event | 工作事实 | SQLite | 是 | CaseCreated, ActionRequested, EvidenceStaled |
| Operational Log | 运行诊断 | 文件 (tracing) | 否 | 请求耗时 842ms, Provider 断连 |
| Client Output | 操作者界面 | stdout/stderr | 否 | "正在调查…", JSON-RPC 响应 |

**Core 禁止直接 println!/eprintln!**。所有输出必须经过 Renderer / Transport 或 tracing。

### stdout/stderr 用途冻结

Human CLI 模式：stdout = 人类可读界面，stderr = 运行日志（默认 warn/error）
Machine RPC 模式：stdout = 纯 JSON-RPC/JSONL 协议，stderr = 运行日志

### Tracing 最小配置

使用 `tracing` + `tracing-subscriber`。推荐字段：
`timestamp`, `level`, `target`, `case_id`, `run_id`, `turn_id`, `action_id`, `event_id`, `provider`, `model`, `operation`, `duration_ms`, `error_type`

默认日志级别 `info`，开发 `debug`，通过 `RUST_LOG` 或配置调整。

### 敏感信息保护

- 默认允许：ID、状态、类型、耗时、计数、错误分类
- 默认禁止：Secret、完整文件内容、完整 Prompt、凭证、未经清理的 Tool 参数和 Provider 原始响应
- 原始 Provider 数据保存需显式开启、单独目录、标记敏感、限制生命周期

### 配置与数据目录

使用系统标准目录库（`directories` / `directories-next`）：

```
config/       → config.toml
data/         → soma.db, cases/, provider-cache/
logs/         → soma.log, soma.log.1
```

支持 CLI 覆盖：`soma --data-dir <path> --config <path>`。测试使用临时目录。

### SQLite Schema 身份

`soma_metadata` 表记录 `database_schema_version`、`created_by_version`、`last_opened_by_version`。
启动时版本检查：版本一致 → 正常打开；版本更高 → Fail Closed；版本更低 → MIGRATION_REQUIRED。
0.1 只有 v1，不实现迁移框架。

### 诊断接口

保留扩展点：`soma doctor` / `soma diagnostics export`。M0 最低版本输出系统信息和最近错误摘要。

### 错误链

错误模型在原有 5 层基础上扩展：

| 层 | 说明 |
|----|------|
| Provider | 模型 API |
| Adapter | Rig 适配层 |
| Engine | Turn Engine 状态机 |
| Policy | 权限决策 |
| Capability | 能力执行 |
| Organ | 底层实现 |
| Store | 持久化 |
| Transport | 通信层 |
| Configuration | 配置错误 |
| EventCodec | 事件解码 |

错误携带 `error_code`、`source_layer`、`recoverability`、`cause_chain`。
