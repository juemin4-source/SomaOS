[package]
name = "soma-ui-protocol"
version = "0.1.0"
edition = "2021"
description = "SomaOS UI Protocol — structured UI events between Runtime and TUI"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
