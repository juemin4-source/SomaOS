# SomaOS Handoff — 2026-07-26 (Phase 2)

> 会话终点：CLI Daily 全部 9 个 Gates 完成 + Dogfood 基线验证
> 下一目标：Dogfood 验收（交互式使用）/ Gate C/D/E 细化 / 推送到 remote

---

## 项目状态

### 仓库

```
Repo:    G:/AI/Claude-Workspace/Projects/SomaOS-Next
HEAD:    8f7a798 (fix: 移除预设对话回复、启动摘要、API Key 环境变量配置)
Branch:  main
Remote:  https://github.com/juemin4-source/SomaOS.git
Status:  0 uncommitted（本轮改动已全部提交）
```

### 里程碑

```
v0.1.0              ✅ tagged (TurnEngine 闭环)
v0.2 Phase 1        ✅ (RunStore, soma-runtime, CLI 切割)
TASK-SOMA-CLI-DAILY-001 ✅ 全部 9 个 Gates 完成
```

---

## 已完成的工作

### TASK-SOMA-CLI-DAILY-001 — 全部 9 个 Gates

| Gate | 范围 | 状态 |
|------|------|------|
| B | workspace/ 模块、WorkspaceContext、detect_workspace()、git 检测、启动摘要 | ✅ |
| C | Shift+Enter 换行、↑↓ 历史导航、粘贴处理 | ✅ |
| D | 工具计时、分类渲染（验证/修改/Git）、format_duration | ✅ |
| E | 输出截断（head/tail）、日志路径提示、错误时更多尾部 | ✅ |
| F | /new /resume /sessions、会话覆盖层、SomaClient 会话切换 | ✅ |
| A | doctor / config / combo list / capability list、统一 CLI 入口 | ✅ |
| G | 退出摘要、Runtime 断开检测与恢复覆盖层、Ctrl+C 行为 | ✅ |
| H | /status /summary /diff /artifacts /compact /review /qa | ✅ |
| I | 终端过小检测、工作目录检查、错误翻译（format_user_error） | ✅ |

### Review 修复（2 轮）

- 未知事件 Fail Closed（tracing::error! + 用户可见 SystemMessage）
- /diff 使用 resolve_git_exe() 替代硬编码
- TurnFailed 应用 format_user_error() 翻译
- Commands::requires_runtime() 替代三路 match
- 删除 `conversational_probe_reply` 机制（不再预设对话回复）
- 移除 TUI 启动摘要 cell（仅保留状态行）
- Windows .bat 启动脚本（soma.bat / soma-quick.bat）

### 测试

```
cargo test --workspace（排除 vertical_slice 集成测试）
63 passed: soma-runtime 15 + soma-client 4 + soma-tui 44
```

---

## API Key 配置

已通过 `setx` 设置系统环境变量：

| 变量 | 值 |
|------|-----|
| `DEEPSEEK_API_KEY` | sk-7bbc238771ed49db9f11d50352095de6 |
| `ANTHROPIC_API_KEY` | 同 key（DeepSeek Anthropic 兼容） |
| `ANTHROPIC_BASE_URL` | https://api.deepseek.com/anthropic |
| `SOMA_PROVIDER` | deepseek |

启动脚本 `soma.bat` / `soma-quick.bat` 中也内嵌了环境变量。

---

## 已知限制

- **Windows Git 路径依赖自动探测** — 非标准位置需设 `SOMA_GIT_PATH`
- **`soma-quick.bat` 预编译检查** — 如果 `target/debug/soma.exe` 不存在会回退到 `cargo run`
- **`.somaos/` 目录残留** — Runtime 异常退出后可能锁住 SQLite，删除 `.somaos/` 再试
- **`soma-runtime.exe` 进程残留** — `cargo build` 失败时通常是有旧进程在运行，`taskkill /f /im soma-runtime.exe`

---

## 下一步建议

### 1. Dogfood 验收（交互式）

在本地终端运行 `soma-quick.bat`，完成：

```
进入项目 → 调查 → 连续讨论 → 修改 → 验证
→ 中途取消一次 → 退出一次 → 重新启动恢复
→ Review → 修复 Finding → QA
```

### 2. Gate 细化

几个 review 发现的可选改进：

- **展开/折叠详情机制** — Gate E 要求但未实现
- **第二次 Ctrl+C 强制退出** — 当前需等取消完成
- **Runtime 重启** — [r] 键目前只退出，未实现真正重启
- **`(task_id, turn_id, created_at)` 数据泥团** — 10+ 处重复

### 3. 推送到 remote

```bash
export HTTPS_PROXY=http://127.0.0.1:7890
git push
```
